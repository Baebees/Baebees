import React from 'react'
import { View, StyleSheet } from 'react-native'

const Followers = ({ route }) => {
    const { followers, context } = route.params
    return(
        <View>

        </View>
    )
}

const styles = StyleSheet.create({

})

export default Followers